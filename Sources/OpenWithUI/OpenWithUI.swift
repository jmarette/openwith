// SwiftUI views shared by OpenWith.app and OpenWithPane.prefPane.
// Populated at the GUI milestone; the target exists from the start so the
// package layout is stable.

import OpenWithCore
import SwiftUI

public enum OpenWithUI {
  public static let bundleID = "com.jmarette.openwith"
}
